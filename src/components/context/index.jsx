import {createContext} from "react";

export const ContextClick = createContext((event, link) => {});
export const ContextPageNameDisplay = createContext("");
export const ContextData = createContext([]);
